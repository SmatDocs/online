
after cloning the repo from github, one important thing, don't use sudo to modify any file permissions.
Make sure all file permissions are for the current user that is not sudo


Follow the steps for ubuntu here 
https://collaboraonline.github.io/post/build-code/#build-code-on-ubuntu


I chose here in the instructions
# Or make it persistent as part of your .bashrc with
echo "export LOCOREPATH=$(pwd)" >> ~/.bashrc && source ~/.bashrc

./configure --enable-silent-rules --with-lokit-path=${LOCOREPATH}/include \
            --with-lo-path=${LOCOREPATH}/instdir \
            --enable-debug --enable-cypress \
            --disable-ssl

also added this
 --disable-ssl
 
sudo apt install libreoffice-script-provider-python python3-uno